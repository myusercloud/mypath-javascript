let names = "Harry";
let age = 19
let driver = true;
if (driver) {
    console.log(names + " is a driver");
} else {
    console.log(names + " is not a driver");
}
console.log(names + " is " + age + " years old");